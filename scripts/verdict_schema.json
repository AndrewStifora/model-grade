{
  "type": "object",
  "properties": {
    "model": {
      "type": "string",
      "enum": ["claude-haiku-4-5", "claude-sonnet-5", "claude-opus-5-5", "claude-opus-5", "claude-fable-5-1"]
    },
    "alias": {
      "type": "string",
      "enum": ["haiku", "sonnet", "opus", "fable"]
    },
    "tier": {"type": "integer", "minimum": 0, "maximum": 3},
    "effort": {
      "type": ["string", "null"],
      "enum": ["low", "medium", "high", "xhigh", "max", null]
    },
    "confidence": {"type": "number", "minimum": 0, "maximum": 1},
    "signals": {
      "type": "object",
      "properties": {
        "scope": {"type": "integer", "minimum": 0, "maximum": 3},
        "horizon": {"type": "integer", "minimum": 0, "maximum": 3},
        "ambiguity": {"type": "integer", "minimum": 0, "maximum": 3},
        "reasoning": {"type": "integer", "minimum": 0, "maximum": 3},
        "verification": {"type": "string", "enum": ["none", "self-check", "checker", "human-review"]},
        "cost_of_error": {"type": "string", "enum": ["trivial", "reversible", "costly"]},
        "context_tokens": {"type": "string", "enum": ["small", "medium", "large"]},
        "modality": {"type": "string", "enum": ["text", "code", "images-simple", "images-dense", "documents"]},
        "domain_flags": {
          "type": "array",
          "items": {"type": "string", "enum": ["security", "bio", "finance-enterprise", "frontend-design", "research-synthesis", "vision"]}
        },
        "output_shape": {"type": "string", "enum": ["structured-short", "code-change", "long-deliverable"]},
        "volume": {"type": "string", "enum": ["one-off", "high-volume", "latency-sensitive"]},
        "latent_markers": {"type": "array", "items": {"type": "string"}},
        "dependencies": {
          "type": "array",
          "items": {"type": "string", "enum": ["skill", "tools", "project", "session"]}
        }
      },
      "required": ["scope", "horizon", "ambiguity", "reasoning", "verification", "cost_of_error", "context_tokens", "modality", "domain_flags", "output_shape", "volume", "latent_markers", "dependencies"],
      "additionalProperties": false
    },
    "why": {"type": "string"},
    "cheaper_alternative": {
      "type": ["object", "null"],
      "properties": {
        "model": {"type": "string", "enum": ["claude-haiku-4-5", "claude-sonnet-5", "claude-opus-5-5", "claude-opus-5", "claude-fable-5-1"]},
        "effort": {"type": ["string", "null"], "enum": ["low", "medium", "high", "xhigh", "max", null]},
        "when": {"type": "string"}
      },
      "required": ["model", "effort", "when"],
      "additionalProperties": false
    },
    "escalate_if": {"type": "array", "items": {"type": "string"}},
    "notes": {"type": "array", "items": {"type": "string"}}
  },
  "required": ["model", "alias", "tier", "effort", "confidence", "signals", "why", "cheaper_alternative", "escalate_if", "notes"],
  "additionalProperties": false
}
